{
  "name": "sportfi-kit-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs-site"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^10.1.8",
    "happy-dom": "^20.8.4",
    "prettier": "^3.8.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0"
  }
}
